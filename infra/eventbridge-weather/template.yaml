AWSTemplateFormatVersion: "2010-09-09"
Description: EventBridge Scheduler -> Lambda -> GitHub repository_dispatch (PAT in SSM SecureString)

Parameters:
  GitHubOwner:
    Type: String
    Default: Yun-Hai-Org
  GitHubRepo:
    Type: String
    Default: pazhou-weather
  EventType:
    Type: String
    Default: weather-report
  PatParameterName:
    Type: String
    Default: /pazhou-weather-dispatcher/github-pat
    Description: Existing SSM SecureString parameter name (create via aws ssm put-parameter)

Resources:
  DispatchLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
        - arn:aws:iam::aws:policy/AmazonSSMReadOnlyAccess

  DispatchFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub "${AWS::StackName}-dispatch"
      Description: Trigger GitHub repository_dispatch for Weather Report
      Runtime: python3.12
      Handler: index.handler
      Timeout: 30
      MemorySize: 128
      Role: !GetAtt DispatchLambdaRole.Arn
      Environment:
        Variables:
          GH_OWNER: !Ref GitHubOwner
          GH_REPO: !Ref GitHubRepo
          EVENT_TYPE: !Ref EventType
          PARAM_NAME: !Ref PatParameterName
      Code:
        ZipFile: |
          import json
          import os
          import urllib.error
          import urllib.request

          import boto3

          ssm = boto3.client("ssm")


          def handler(event, context):
              owner = os.environ["GH_OWNER"]
              repo = os.environ["GH_REPO"]
              event_type = os.environ.get("EVENT_TYPE", "weather-report")
              param_name = os.environ["PARAM_NAME"]

              token = ssm.get_parameter(
                  Name=param_name, WithDecryption=True
              )["Parameter"]["Value"].strip()
              if not token:
                  raise RuntimeError("GitHub PAT parameter is empty")

              url = f"https://api.github.com/repos/{owner}/{repo}/dispatches"
              body = json.dumps({"event_type": event_type}).encode("utf-8")
              req = urllib.request.Request(
                  url,
                  data=body,
                  method="POST",
                  headers={
                      "Accept": "application/vnd.github+json",
                      "Authorization": f"Bearer {token}",
                      "X-GitHub-Api-Version": "2022-11-28",
                      "Content-Type": "application/json",
                      "User-Agent": "pazhou-weather-eventbridge-dispatcher",
                  },
              )

              try:
                  with urllib.request.urlopen(req, timeout=20) as resp:
                      status = resp.status
                      resp_body = resp.read().decode("utf-8", errors="replace")
              except urllib.error.HTTPError as exc:
                  resp_body = exc.read().decode("utf-8", errors="replace")
                  print(json.dumps({
                      "status": exc.code,
                      "owner": owner,
                      "repo": repo,
                      "eventType": event_type,
                      "body": resp_body[:500],
                  }))
                  raise RuntimeError(f"GitHub dispatch failed: {exc.code} {resp_body}") from exc

              print(json.dumps({
                  "status": status,
                  "owner": owner,
                  "repo": repo,
                  "eventType": event_type,
                  "body": resp_body[:500],
              }))
              return {"ok": True, "status": status}

  SchedulerInvokeRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: scheduler.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: InvokeDispatchLambda
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - lambda:InvokeFunction
                Resource: !GetAtt DispatchFunction.Arn

  MorningSchedule:
    Type: AWS::Scheduler::Schedule
    Properties:
      Name: !Sub "${AWS::StackName}-morning"
      Description: Weather Report morning slot (Beijing 06:05)
      FlexibleTimeWindow:
        Mode: "OFF"
      ScheduleExpression: cron(5 6 * * ? *)
      ScheduleExpressionTimezone: Asia/Shanghai
      State: ENABLED
      Target:
        Arn: !GetAtt DispatchFunction.Arn
        RoleArn: !GetAtt SchedulerInvokeRole.Arn
        RetryPolicy:
          MaximumEventAgeInSeconds: 3600
          MaximumRetryAttempts: 2

  AfternoonSchedule:
    Type: AWS::Scheduler::Schedule
    Properties:
      Name: !Sub "${AWS::StackName}-afternoon"
      Description: Weather Report afternoon slot (Beijing 15:05)
      FlexibleTimeWindow:
        Mode: "OFF"
      ScheduleExpression: cron(5 15 * * ? *)
      ScheduleExpressionTimezone: Asia/Shanghai
      State: ENABLED
      Target:
        Arn: !GetAtt DispatchFunction.Arn
        RoleArn: !GetAtt SchedulerInvokeRole.Arn
        RetryPolicy:
          MaximumEventAgeInSeconds: 3600
          MaximumRetryAttempts: 2

Outputs:
  DispatchFunctionName:
    Value: !Ref DispatchFunction
  DispatchFunctionArn:
    Value: !GetAtt DispatchFunction.Arn
  GitHubPatParameterName:
    Value: !Ref PatParameterName
  MorningScheduleName:
    Value: !Ref MorningSchedule
  AfternoonScheduleName:
    Value: !Ref AfternoonSchedule
